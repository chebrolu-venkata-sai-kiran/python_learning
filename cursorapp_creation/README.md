# cursorapp_creation – To-Do Web App

Simple To-Do list (HTML + CSS). This app was **generated by the “agent for creating app” flow** in **Chapter 18** (`18_First_Agent/04_agent_for_creating_app.py`).

Open `index.html` in a browser to use it. No backend; static front-end only.
